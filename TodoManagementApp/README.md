# TodoManagementApp

Simple WinForms Todo management scaffold using MongoDB and AccessCode login.

To build:

```powershell
cd TodoManagementApp
dotnet restore
dotnet build -c Release
```

Default config: `Config/appsettings.json` (MongoDB connection, DB name, admin seed)
